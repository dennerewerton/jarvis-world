diff --git a/src/main.jsx b/src/main.jsx
--- a/src/main.jsx
+++ b/src/main.jsx
@@ -46,7 +46,8 @@ class RuntimeErrorBoundary extends React.Component {
 
   componentDidCatch(error) {
     console.error('Webaverse render failed', error);
-    window.dispatchEvent(new Event('jarvis-webaverse-runtime-error'));
+    const message = error instanceof Error ? error.message : String(error || 'Erro de renderização desconhecido.');
+    window.dispatchEvent(new CustomEvent('jarvis-webaverse-runtime-error', {detail: {message}}));
   }
 
   render() {
diff --git a/src/components/app/App.jsx b/src/components/app/App.jsx
--- a/src/components/app/App.jsx
+++ b/src/components/app/App.jsx
@@ -67,7 +67,8 @@ const Canvas = ({
 
             _startApp( app, canvasRef.current ).catch(error => {
                 console.error('Webaverse runtime startup failed', error);
-                window.dispatchEvent(new Event('jarvis-webaverse-runtime-error'));
+                const message = error instanceof Error ? error.message : String(error || 'Erro de inicialização desconhecido.');
+                window.dispatchEvent(new CustomEvent('jarvis-webaverse-runtime-error', {detail: {message}}));
             });
 
         }
diff --git a/src/jarvis-compat/ActivityShell.jsx b/src/jarvis-compat/ActivityShell.jsx
--- a/src/jarvis-compat/ActivityShell.jsx
+++ b/src/jarvis-compat/ActivityShell.jsx
@@ -116,9 +116,13 @@ export const ActivityShell = ({children}) => {
   }, [start]);
 
   useEffect(() => {
-    const handleRuntimeFailure = () => {
+    const handleRuntimeFailure = event => {
       clearActivitySession();
-      setState({stage: 'runtime_error'});
+      const detail = typeof event?.detail?.message === 'string' ? event.detail.message.trim() : '';
+      setState({
+        stage: 'runtime_error',
+        message: detail ? `Detalhe técnico: ${detail}` : undefined,
+      });
     };
     const handleContextLost = event => {
       event.preventDefault();
