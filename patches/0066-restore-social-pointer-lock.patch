diff --git a/io-manager.js b/io-manager.js
--- a/io-manager.js
+++ b/io-manager.js
@@ -771,4 +771,7 @@ ioManager.mousedown = e => {
-  } else {
+  } else if ((changedButtons & 1) && (e.buttons & 1)) { // left
+    // Enter camera control without restoring editor gizmos or object menus.
+    cameraManager.requestPointerLock();
+  } else {
     // Transform gizmos and the scene context menu are development-only.
   }
   if ((changedButtons & 4) && (e.buttons & 4)) { // middle
